# Age Gate App (Micro Project 01)

- Variables / Operators / Conditionals + DOM output
  🔑 Goal: Teach state + decisions + visible output

## What you built

A simple age checker that takes the user's age and shows a decision message.

Example outcomes:

- Denied (under 13)
- Warning / restricted access (13–17)
- Allowed (18+)
- Full access (21+)

---

## What you learned (JavaScript)

- How to read input values from the DOM
- How to convert strings to numbers using `Number()`
- How to compare numbers using operators (`<`, `>=`)
- How to use `if / else if / else` to control program flow
- How to update the UI with `textContent`
- How to change UI styles by adding/removing CSS classes

---
